diff --git a/group.services.yml b/group.services.yml
--- a/group.services.yml
+++ b/group.services.yml
@@ -112,9 +112,14 @@
     arguments: ['@private_key', '@cache.static', '@group_permission.calculator', '@entity_type.manager', '@database']
 
   # Wrappers for Flexible permissions services.
+  group_permission.chain_calculator:
+    class: 'Drupal\flexible_permissions\ChainPermissionCalculator'
+    arguments: ['@variation_cache.group_permissions', '@variation_cache.group_permissions_memory', '@cache.memory', '@account_switcher']
+    tags:
+      - { name: 'service_collector', call: 'addCalculator', tag: 'flexible_permission_calculator' }
   group_permission.calculator:
     class: 'Drupal\group\Access\GroupPermissionCalculator'
-    arguments: ['@flexible_permissions.chain_calculator']
+    arguments: ['@group_permission.chain_calculator']
   group_permission.checker:
     class: 'Drupal\group\Access\GroupPermissionChecker'
     arguments: ['@group_permission.calculator', '@group.membership_loader']
@@ -125,6 +130,14 @@
     arguments: ['@entity_type.manager', '@group.membership_loader']
     tags:
       - { name: flexible_permission_calculator, priority: -100 }
+  variation_cache.group_permissions:
+    class: 'Drupal\Core\Cache\VariationCacheInterface'
+    factory: ['@variation_cache_factory', 'get']
+    arguments: ['memory']
+  variation_cache.group_permissions_memory:
+    class: 'Drupal\Core\Cache\VariationCacheInterface'
+    factory: ['@variation_cache_factory', 'get']
+    arguments: ['memory']
   group_permission.synchronized_calculator:
     class: 'Drupal\group\Access\SynchronizedGroupPermissionCalculator'
     arguments: ['@entity_type.manager']
