diff --git a/src/Boot/DrupalBoot8.php b/src/Boot/DrupalBoot8.php
index baab579..8f5da16 100644
--- a/src/Boot/DrupalBoot8.php
+++ b/src/Boot/DrupalBoot8.php
@@ -241,7 +241,13 @@ class DrupalBoot8 extends DrupalBoot
         $serviceFinder = new LegacyServiceFinder($moduleHandler, Drush::config());
         $drushServiceFiles = $serviceFinder->getDrushServiceFiles();
         $legacyServiceInstantiator = new LegacyServiceInstantiator($container, $this->logger);
-        $legacyServiceInstantiator->loadServiceFiles($drushServiceFiles);
+        try {
+            $legacyServiceInstantiator->loadServiceFiles($drushServiceFiles);
+        } catch (\Throwable $e) {
+            $this->logger->warning('Skipping legacy drush.services.yml bootstrap due to error: ' . $e->getMessage());
+            $drushServiceFiles = [];
+            $legacyServiceInstantiator = new LegacyServiceInstantiator($container, $this->logger);
+        }
 
         // Find the containerless commands, and command info alterers
         $bootstrapCommandClasses = $this->serviceManager->bootstrapCommandClasses();
diff --git a/src/Commands/core/UpdateDBCommands.php b/src/Commands/core/UpdateDBCommands.php
index 7629d8c..8cd9507 100644
--- a/src/Commands/core/UpdateDBCommands.php
+++ b/src/Commands/core/UpdateDBCommands.php
@@ -471,10 +471,16 @@ class UpdateDBCommands extends DrushCommands
      */
     public static function cacheRebuild(): void
     {
-        drupal_flush_all_caches();
-        \Drupal::service('kernel')->rebuildContainer();
-        // Load the module data which has been removed when the container was
-        // rebuilt.
+        try {
+            drupal_flush_all_caches();
+            \Drupal::service('kernel')->rebuildContainer();
+        } catch (\Throwable $e) {
+            Drush::logger()->warning('Skipping inter-update cache rebuild due to error: ' . $e->getMessage());
+            return;
+        }
+
+        // Load the module data which has been removed when the container was
+        // rebuilt.
         $module_handler = \Drupal::moduleHandler();
         $module_handler->loadAll();
         $module_handler->invokeAll('rebuild');
